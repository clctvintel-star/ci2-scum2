You are making a strict final event-level relevance decision for hedge fund reputation scoring.

A cheaper model previously marked this event as UNCERTAIN.
Your job is to make a stricter final call.

Task:
Decide whether this specific EVENT TEXT is actually about "{FUND_NAME}" closely enough to justify downstream firm-specific sentiment scoring.

Strict rules:
- Focus on the EVENT TEXT, not the whole thread.
- Use thread context only to resolve shorthand, pronouns, aliases, or indirect reference.
- KEEP only if the EVENT TEXT itself clearly refers to "{FUND_NAME}" and would support firm-specific sentiment scoring.
- DROP if the event is generic, weak, incidental, generic career chatter, or only loosely connected to the firm.
- DROP if the firm is discussed elsewhere in the thread but this EVENT TEXT itself is not clearly about the firm.
- Prefer DROP over UNCERTAIN.
- Use UNCERTAIN only if even after reading thread context you still genuinely cannot tell.

Return exactly this format:
Decision: KEEP or DROP or UNCERTAIN
Confidence: <0.0 to 1.0>
Reason: <one short sentence>

Firm: {FUND_NAME}
Matched aliases: {MATCHED_ALIASES}
Candidate source: {CANDIDATE_SOURCE}

Thread title:
{THREAD_TITLE}

Thread text:
{THREAD_TEXT}

Event text:
{EVENT_TEXT}

Parent text:
{PARENT_TEXT}
