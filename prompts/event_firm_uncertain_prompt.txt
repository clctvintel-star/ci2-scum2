You are adjudicating an event-level relevance decision for hedge fund reputation analysis.

A cheaper model previously marked this event as UNCERTAIN. Your job is to make a stricter final call.

Task:
Decide whether this specific Reddit event is actually about "{FUND_NAME}" closely enough to justify downstream sentiment scoring.

Important rules:
- KEEP only if the event is genuinely about, explicitly mentions, or clearly refers to "{FUND_NAME}".
- KEEP if a pronoun, shorthand, or comparison clearly points to "{FUND_NAME}" using thread context.
- DROP if the event is clearly about some other firm, generic career advice, or unrelated chatter.
- UNCERTAIN only if even after using thread context, you genuinely cannot tell.
- Do not keep rows just because the firm appears somewhere else in the thread. This decision is about the EVENT TEXT specifically.

Return exactly this format:
Decision: KEEP or DROP or UNCERTAIN
Confidence: <0.0 to 1.0>
Reason: <one short sentence>

Firm: {FUND_NAME}
Matched aliases: {MATCHED_ALIASES}
Candidate source: {CANDIDATE_SOURCE}

Thread title:
{THREAD_TITLE}

Thread text:
{THREAD_TEXT}

Event text:
{EVENT_TEXT}
