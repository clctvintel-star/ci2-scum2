You are scoring ONE Reddit event for hedge fund reputation analysis.

Task:
Evaluate how this specific EVENT TEXT affects the perceived reputation of “{FUND_NAME}” from the perspective of a potential job applicant.

Core rule:
The EVENT TEXT is the primary unit of analysis.

Use THREAD TITLE, THREAD TEXT, and PARENT TEXT only to resolve:
	•	shorthand
	•	aliases
	•	pronouns
	•	sarcasm
	•	comparisons
	•	inherited context

Do NOT score the whole thread.

⸻

CONTEXT VALIDATION GATE (CRITICAL)

Before scoring, determine whether the EVENT TEXT is a valid firm-specific sentiment event.

A valid event must satisfy at least one:
	1.	The EVENT TEXT directly evaluates “{FUND_NAME}”
	2.	The EVENT TEXT clearly refers to “{FUND_NAME}” via context (parent/thread) AND modifies or reacts to that evaluation
	3.	The EVENT TEXT adds new firm-specific information or evaluation about “{FUND_NAME}” (not general advice or discussion)

If NONE of the above are true:

Return:
sentiment = null
confidence = 0.0

The validation gate OVERRIDES all later scoring rules.
If the event is not valid, you MUST return null and STOP.
Do NOT continue to scoring.

Additional constraints:
	•	Do NOT score generic advice, resume discussion, academic discussion, or industry commentary unless it clearly resolves to “{FUND_NAME}”
	•	Do NOT inherit sentiment from PARENT TEXT unless the EVENT TEXT actively engages with it
	•	If the EVENT TEXT mentions “{FUND_NAME}” but contains no evaluative signal and does not modify an existing evaluation → return null

⸻

CONTEXT-DEPENDENT RESPONSE RULE

If the EVENT TEXT is brief, vague, or does not contain a clear standalone opinion:
	•	Do NOT evaluate it independently
	•	Use PARENT TEXT as the primary source of meaning
	•	Treat the EVENT TEXT as modifying, reinforcing, questioning, reacting to, or narrowing the meaning of the PARENT TEXT

Interpretation rules:
	•	If the PARENT TEXT provides clear directional sentiment about “{FUND_NAME}”, inherit that direction and adjust magnitude based on how strongly the EVENT TEXT reinforces or changes it
	•	If the EVENT TEXT weakens, hedges, narrows, or distances itself → reduce magnitude toward neutral
	•	If the EVENT TEXT is merely reactive and does not actively engage → sentiment should be very small or 0.0
	•	If the PARENT TEXT is missing, unclear, or not about “{FUND_NAME}” → return:
sentiment = 0.0
confidence = 0.1–0.2

Important:
Do NOT amplify weak or incidental mentions from PARENT TEXT into meaningful sentiment.

⸻

Scope rules:
	•	Score sentiment ABOUT “{FUND_NAME}” only
	•	Do NOT score other firms unless comparison clearly changes perception of “{FUND_NAME}”
	•	A context row may still be relevant even if the EVENT TEXT does not repeat the firm name, if the broader thread clearly establishes the subject

⸻

Firm-link discipline:
	•	If the EVENT TEXT is only about hedge funds, prop shops, multistrats, HFTs, or buy-side roles generally, do NOT convert that into firm-specific sentiment unless “{FUND_NAME}” is clearly the referent
	•	If the row is category-level rather than firm-specific → sentiment = 0.0
	•	Do NOT infer firm-specific reputation from generic sector commentary

⸻

Range usage rule:
	•	Use full range from -1.0 to 1.0 when warranted
	•	Do NOT cluster meaningful signals near 0.0
	•	If “{FUND_NAME}” appears in an evaluative context, scores between -0.05 and +0.05 should be rare
	•	Mild signals → use roughly ±0.1 to ±0.3
	•	Strong employer signals → use ±0.6 to ±0.9
	•	Reserve 0.0 for genuinely nondiagnostic cases
	•	Do NOT compress directional signals toward 0.0 due to low confidence

⸻

Job-seeker framing:

Signals include:
	•	prestige
	•	compensation
	•	performance
	•	culture
	•	hiring difficulty
	•	selectivity
	•	elite status
	•	exits
	•	stability
	•	layoffs
	•	burnout
	•	toxicity
	•	training quality
	•	internal mobility
	•	return offers
	•	turnover
	•	politics
	•	desirability

⸻

Scoring rules:
	•	Use directional scores when signal exists
	•	Do NOT default to 0.0 for mild signals
	•	Score NET effect when mixed
	•	Use smaller magnitudes for weak signals
	•	If any real evaluative signal exists → DO NOT collapse to 0.0

⸻

Weak-signal constraint:

Indirect signals (lists, mentions, salience):
	•	must stay near neutral
	•	must carry low confidence
	•	must NOT be treated as strong signals

⸻

Critical rule:

If the event is relevant to “{FUND_NAME}”, you MUST return a numeric sentiment score.

Relevant but nondiagnostic:
sentiment = 0.0
confidence > 0.0

Return null ONLY if the event fails the validation gate.

⸻

Prestige-list caution:
	•	Weak prestige → +0.01 to +0.05
	•	Do NOT assign strong positive from list inclusion alone
	•	Purely factual → 0.0

⸻

Confidence guidance:

High → explicit firm-specific signal
Medium → indirect but clear
Low → weak but real

Confidence = 0 ONLY if unrelated (null case)

⸻

IMPORTANT:

EVENT TEXT may depend on PARENT TEXT for meaning.
Always resolve context BEFORE scoring.

⸻

Return valid JSON only.
No markdown.
No extra text.

Return exactly:

{{ 
"sentiment": -0.5,
"confidence": 0.7,
"explanation": "brief explanation"
}}

⸻

Firm: {FUND_NAME}
Matched aliases: {MATCHED_ALIASES}
Candidate source: {CANDIDATE_SOURCE}

Thread title:
{THREAD_TITLE}

Thread text:
{THREAD_TEXT}

Parent text:
{PARENT_TEXT}

Event text:
{EVENT_TEXT}
